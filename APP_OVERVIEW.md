# تطبيق المعلم القرآني (myhalaqat) — وثيقة تعريفية شاملة

## 1. نظرة عامة

**تطبيق المعلم القرآني** هو تطبيق جوال لتطبيق أندرويد، مصمم لمعلمي حلقات تحفيظ القرآن الكريم. يساعد المعلم على إدارة حلقته القرآنية رقمياً، من خلال تسجيل الحضور والغياب، ومتابعة حفظ الطلاب، وطلبات السبر (الاختبار الشفهي)، مع دعم كامل للعمل **دون اتصال بالإنترنت** (Offline-First).

التطبيق موجه لمعلمي حلقات القرآن في المساجد والجمعيات، حيث الإنترنت قد لا يكون متوفراً بشكل دائم في مكان الحلقة.

---

## 2. الأهداف الرئيسية

| الهدف | الوصف |
|-------|-------|
| **تسجيل الحضور بسهولة** | تسجيل حضور/غياب/استئذان الطلاب بتاريخ معين مع التحقق من أيام الدورة |
| **متابعة الحفظ** | تسجيل تسميع الطلاب (سورة + آيات + نوع: جديد/مراجعة + تقييم: ممتاز/جيد/إعادة) |
| **طلبات السبر** | إرسال طلبات اختبار شفهي للمشرف مع تحديد الجزء ونوع السبر |
| **العمل دون إنترنت** | حفظ كل العمليات محلياً وإرسالها تلقائياً عند توفر الاتصال |
| **عدم فقدان البيانات** | أي إدخال للمعلم يُحفظ فوراً محلياً ولا يضيع أبداً |
| **مزامنة خلفية ذكية** | إرسال البيانات تلقائياً حتى عند إغلاق التطبيق |
| **شفافية الحالة** | المعلم يعرف دائماً: ما أُرسل، ما ينتظر، ما فشل ولماذا |

---

## 3. التقنيات المستخدمة

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| Flutter | 3.41.9 | إطار العمل الرئيسي |
| Dart | 3.11.5 | لغة التطوير |
| Dio | ^5.9.2 | HTTP client مع Interceptors (JWT) |
| SQLite (sqflite) | ^2.4.2+1 | التخزين المحلي Offline-First |
| SharedPreferences | ^2.5.5 | كاش سريع key-value |
| connectivity_plus | ^7.1.1 | مراقبة حالة الاتصال |
| WorkManager | ^0.9.0+3 | مزامنة دورية في الخلفية |
| flutter_background_service | ^5.1.0 | خدمة خلفية موثوقة (Foreground Service) |
| flutter_dotenv | ^6.0.1 | متغيرات البيئة (BASE_URL) |
| intl / flutter_localization | — | دعم العربية والتقويم الهجري |
| google_fonts | ^8.1.0 | خط Cairo للعربية |

**المتطلبات**: Android 5.0+ (API 21) حتى أحدث الإصدارات.

---

## 4. المعمارية (Architecture)

```
┌─────────────────────────────────────────────┐
│         طبقة العرض (Presentation)           │
│   شاشات Flutter + StatefulWidget + setState │
├─────────────────────────────────────────────┤
│         طبقة الخدمات (Services)             │
│   attendance_service / memorization_service │
│   saber_service / circle_service / ...      │
├─────────────────────────────────────────────┤
│         طبقة المزامنة (Sync Layer)          │
│   SyncManager (محرك المزامنة الرئيسي)       │
│   BackgroundSyncService (خدمة الخلفية)      │
├─────────────────────────────────────────────┤
│         طبقة البيانات (Data Layer)          │
│   SQLite (pending tables + cache)           │
│   SharedPreferences (cache سريع)            │
│   API (Django REST) عبر Dio + JWT           │
└─────────────────────────────────────────────┘
```

### نمط العمل الأساسي (Offline-First)

```
عملية المعلم (حضور/حفظ/سبر)
        ↓
حفظ فوري في SQLite المحلي (pending table)
        ↓
تحديث واجهة المستخدم والعدادات فوراً
        ↓
محاولة إرسال مباشر (إذا متصل) أو انتظار المزامنة
        ↓
مزامنة تلقائية: عند عودة الإنترنت / كل 5 دقائق / عند فتح التطبيق
        ↓
تحديث الحالة: synced ✅ / pending ⏳ / failed ❌ مع سبب الخطأ
```

---

## 5. أقسام التطبيق (الميزات)

### 5.1 تسجيل الدخول
- شاشة دخول بثيم نهاري دائماً
- JWT tokens مع تجديد تلقائي (refresh token)
- حفظ الجلسة محلياً

### 5.2 الشاشة الرئيسية (دوراتي)
- قائمة الدورات المسندة للمعلم مع حالة كل دورة (نشط/منتهي)
- شريط حالة المزامنة (محدثة / معلقة / فاشلة / غير متصل)
- بطاقة "حالة المزامنة" تظهر دائماً مع عدد المعلقات

### 5.3 الحلقات
- قائمة حلقات كل دورة
- بطاقة الحلقة مع اسم المعلم
- صفحة تفاصيل الحلقة مع الإجراءات (حضور/حفظ/سبر)

### 5.4 الحضور والغياب
- تسجيل حضور/غياب/استئذان دفعة واحدة لجميع الطلاب
- بحث وترتيب الطلاب
- التحقق من أيام الدورة (لا يقبل أيام خارج الدورة)
- سجل الحضور مع فلترة بالتاريخ وشارات الحالة (تم الإرسال/بانتظار المزامنة/فشل)

### 5.5 الحفظ
- تسجيل حفظ لكل طالب (سورة، من آية، إلى آية، نوع، تقييم، ملاحظات)
- تذكر آخر سورة وآيات لكل طالب (SharedPreferences)
- حفظ وإرسال فوري عند الاتصال
- سجل الحفظ مع فلترة التاريخ محلياً

### 5.6 طلبات السبر
- إرسال طلب سبر (الجزء 1-30، النوع: جديد/مراجعة، ملاحظات)
- سجل الطلبات مع شارات المزامنة وحالة الإدارة (مكتمل/مرفوض/معلق)

### 5.7 سجل المزامنة (صفحة خاصة)
- 4 تبويبات: الكل / معلق / فاشل / متزامن
- بحث باسم الطالب وفلترة بالتاريخ وترتيب
- تحديد وحذف جماعي، إعادة محاولة فردية أو جماعية
- عرض أسباب الأخطاء مترجمة للعربية

### 5.8 التقارير والسجلات
- تقارير الغياب، تقارير أسبوعية وشاملة لكل طالب
- سجل النقاط والاختبارات والأجزاء
- لوحة إحصائيات المعلم

### 5.9 الملف الشخصي
- بيانات المعلم
- تبديل الوضع الليلي/النهاري
- رقم الإصدار الديناميكي
- تسجيل الخروج

---

## 6. نظام المزامنة (التفاصيل التقنية)

### 6.1 جداول المزامنة المحلية (SQLite)

| الجدول | الوظيفة |
|--------|---------|
| pending_attendance | سجلات الحضور غير المرسلة |
| pending_memorizations | سجلات الحفظ غير المرسلة |
| pending_quiz_requests | طلبات السبر غير المرسلة |

كل جدول يحمل: `sync_status` (pending/sending/synced/failed)، `retry_count`، `action` (create/update/delete)، `server_id`، `last_attempt_at`، `last_error`.

### 6.2 محرك المزامنة (SyncManager)

- **syncAll()**: نقطة الدخول لكل المزامنة — يرسل الحضور (batch)، الحفظ (batch)، السبر (فردي)
- **طابور ذكي**: إذا المزامنة تعمل، تُسجل طلبات إضافية وتُنفذ بعدها (بدون فقدان)
- **معالجة التكرار**: إذا السيرفر رد "السجل موجود مسبقاً" أو "يجب أن تشكل مجموعة فريدة"، يعتبر السجل مُرسلاً (synced) وليس فشلاً
- **ترجمة الأخطاء**: رسائل Django تترجم لعربية واضحة (مثال: "الطالب غير موجود في النظام")
- **مهلة زمنية**: 15 ثانية لكل طلب HTTP ثم فشل فوري (لا تعليق للأبد)
- **إعادة محاولة**: أخطاء timeout/500 تعاد محاولتها بعد 30 ثانية تصاعدياً
- **تنظيف**: السجلات المتزامنة تُحذف بعد 7 أيام

### 6.3 مصادر المزامنة التلقائية

| المصدر | التوقيت |
|--------|---------|
| فتح التطبيق (AppLifecycleListener) | فوري |
| عودة الإنترنت (connectivity_plus) | فوري |
| مؤقت داخل التطبيق | كل 30 ثانية |
| خدمة الخلفية (Foreground Service) | كل 5 دقائق |
| WorkManager (احتياطي) | كل 15 دقيقة |

### 6.4 إشعار الخدمة الخلفية

- يظهر فقط عند وجود طلبات معلقة
- يعرض العدد والحالة (مثال: "⚠️ 5 فشل — 3 معلق")
- يختفي تلقائياً بعد اكتمال المزامنة (خلال 30 ثانية)
- لا يظهر أثناء استخدام التطبيق (يظهر فقط في الخلفية)

---

## 7. تدفقات المستخدم الرئيسية

### تسجيل الحضور
```
فتح الحلقة ← الضغط على "حضور/غياب" ← تحديد التاريخ
← تحديد حالة كل طالب (حاضر/غائب/مستأذن) ← حفظ
← إرسال فوري (إذا متصل) أو حفظ محلي ← متابعة الحالة في سجل المزامنة
```

### تسجيل الحفظ
```
فتح الحلقة ← "تسجيل حفظ" ← اختيار الطالب ← السورة والآيات (تذكر تلقائي)
← النوع والتقييم ← حفظ وإرسال ← متابعة في سجل الحفظ
```

### طلب السبر
```
فتح الحلقة ← "طلب سبر" ← اختيار الطالب ← الجزء والنوع والملاحظات ← إرسال
← ظهور الطلب في سجل السبر مع شارة الحالة
```

---

## 8. معالجة الأخطاء (تجربة المستخدم)

| الحالة | الرسالة للمستخدم |
|--------|------------------|
| لا يوجد اتصال | "تعذر الاتصال بالسيرفر — تحقق من اتصالك بالإنترنت" |
| انتهت المهلة | "انتهت مهلة الاتصال — تحقق من اتصالك وحاول مرة أخرى" |
| بيانات غير صالحة (400) | "الطالب: السجل غير موجود في النظام" |
| انتهت الجلسة (401) | "انتهت صلاحية الجلسة — يرجى تسجيل الدخول مرة أخرى" |
| السجل موجود مسبقاً | يُعتبر ناجحاً ويُحذف من قائمة الانتظار |
| خطأ في السيرفر (500) | "خطأ داخلي في السيرفر — حاول مرة أخرى لاحقاً" |

---

## 9. الإصدارات والحجم

| الإصدار | الحجم | ملاحظات |
|---------|-------|---------|
| 1.0 | ~28MB | النسخة الأولى |
| 1.1 | 23-26MB | حسب المعالج (arm64 / armeabi-v7a) |

التطبيق مبني بـ `--split-per-abi` لتقليل الحجم، مع دعم Android 5.0+.

---

## 10. الخطط المستقبلية

- [ ] تقارير PDF قابلة للتصدير
- [ ] إدارة الطلاب (إضافة/تعديل من التطبيق)
- [ ] إشعارات Firebase (FCM) للتحديثات الفورية
- [ ] نظام التحديث الإجباري عبر السيرفر
- [ ] دعم أجهزة iOS
- [ ] إدارة الحالة بـ Riverpod/Provider (تحسين المعمارية)
- [ ] تحسين المزامنة عبر WebSocket

---

## 11. نقاط قوة التطبيق

1. **Offline-First حقيقي**: كل البيانات تُحفظ محلياً قبل أي إرسال
2. **لا فقدان بيانات**: مهما حدث، إدخال المعلم محفوظ
3. **مزامنة خلفية موثوقة**: تعمل حتى على أجهزة Xiaomi/Huawei (Foreground Service)
4. **واجهة عربية كاملة**: رسائل مفهومة وغير تقنية
5. **بساطة**: مصمم لمعلم غير تقني
6. **تتبع كامل**: المعلم يعرف حالة كل سجل (أُرسل/معلق/فشل+سبب)

---

*آخر تحديث: الإصدار 1.1*
